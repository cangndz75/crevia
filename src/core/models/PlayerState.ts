export type PlayerState = {
  name?: string;
  xp: number;
  xpToNextLevel: number;
  authorityPoints: number;
  level: number;
  title: string;
  role: string;
  notificationCount: number;
  streakDays: number;
};
