import { eventDetail } from '@/features/events/theme/eventDetailTokens';

export const planningDetailLayout = {
  screenPadding: eventDetail.screenPadding,
  cardRadius: eventDetail.smallRadius,
  innerRadius: 12,
  gap: 10,
} as const;
